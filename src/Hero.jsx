import React from 'react';

const Hero = () => {
    return (
        <div className="container">
            <div className="hero">
                Welcome
            </div>
        </div>
    );
};

export default Hero;
